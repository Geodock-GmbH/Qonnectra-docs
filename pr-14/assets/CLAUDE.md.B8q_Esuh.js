import{_ as n,c as i,o as t,ah as d}from"./chunks/framework.oAolcYhj.js";const p=JSON.parse('{"title":"CLAUDE.md","description":"","frontmatter":{},"headers":[],"relativePath":"CLAUDE.md","filePath":"CLAUDE.md"}'),r={name:"CLAUDE.md"};function s(o,e,a,c,l,h){return t(),i("div",null,[...e[0]||(e[0]=[d(`<h1 id="claude-md" tabindex="-1">CLAUDE.md <a class="header-anchor" href="#claude-md" aria-label="Permalink to &quot;CLAUDE.md&quot;">​</a></h1><p>Leitfaden für Claude Code in diesem Repository.</p><p><strong>Sprache: Alles Nutzersichtbare ist Deutsch</strong> – Handbuchtexte, Alt-Texte, UI-Zitate, Skript-Ausgaben und Code-Kommentare. Commit-Messages sind Englisch.</p><h2 id="was-dieses-repo-ist" tabindex="-1">Was dieses Repo ist <a class="header-anchor" href="#was-dieses-repo-ist" aria-label="Permalink to &quot;Was dieses Repo ist&quot;">​</a></h2><p>VitePress-Site für <strong>Qonnectra</strong> (Open-Source-Netzdokumentation für kommunale Infrastrukturen, Geodock GmbH &amp; plan[neo] GmbH, AGPL-3.0). Zwei Inhaltsstränge:</p><ul><li><strong>Landing Page</strong>: <code>index.md</code>, <code>services/</code>, <code>contact/</code>, <code>imprint/</code>, <code>privacy/</code></li><li><strong>Handbuch</strong>: <code>manual/</code> – der Schwerpunkt der laufenden Arbeit</li></ul><p>Das Handbuch ist in drei Teile gegliedert (Zielgruppen siehe <code>manual/index.md</code>):</p><table tabindex="0"><thead><tr><th>Verzeichnis</th><th>Teil</th><th>Kapitel</th></tr></thead><tbody><tr><td><code>manual/index.md</code></td><td>Einführung</td><td>1</td></tr><tr><td><code>manual/teil-a-anwenderhandbuch/</code></td><td>A – Anwenderhandbuch (Webanwendung, keine GIS-Kenntnisse)</td><td>2–7, reserviert 8–13</td></tr><tr><td><code>manual/teil-b-betrieb-admin-qgis/</code></td><td>B – Betrieb, Administration, QGIS</td><td>14, reserviert 15–16</td></tr><tr><td><code>manual/teil-c-entwickler-systemdokumentation/</code></td><td>C – Entwickler- und Systemdokumentation</td><td>17 ff.</td></tr></tbody></table><p>Die Nummernlücken sind bewusst – neue Kapitel füllen sie auf, statt bestehende Nummern zu verschieben. Kapitelnummer im H1 <strong>und</strong> im Dateinamen-Präfix müssen übereinstimmen (<code>06-rohrverwaltung.md</code> → <code># 6. Rohrverwaltung</code>), die Sidebar wird aus Dateinamen-Reihenfolge + H1 generiert (<code>vitepress-sidebar</code>).</p><h2 id="befehle" tabindex="-1">Befehle <a class="header-anchor" href="#befehle" aria-label="Permalink to &quot;Befehle&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # http://localhost:5173</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # BASE_PATH=&quot;/Qonnectra-docs/&quot; in CI</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint:spelling</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # cspell (de+en) – muss vor jedem Commit grün sein</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:setup</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # Login-Zustand nach auth-state.json schreiben</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # Playwright-Specs in tests/</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/setup-local-qonnectra.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # lokale Qonnectra-Instanz bauen/starten</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/setup-local-qonnectra.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reset</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Daten + Secrets verwerfen, neu aufbauen</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/install-local-ca.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # Dev-CA einmal pro Rechner importieren</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Neue deutsche Fachwörter, die cspell nicht kennt, alphabetisch in <code>.cspell.json</code> unter <code>words</code> einsortieren – nicht per Inline-Kommentar unterdrücken.</p><h2 id="schreibstil-des-handbuchs" tabindex="-1">Schreibstil des Handbuchs <a class="header-anchor" href="#schreibstil-des-handbuchs" aria-label="Permalink to &quot;Schreibstil des Handbuchs&quot;">​</a></h2><p>Verbindlich, abgeleitet aus den bestehenden Kapiteln. Neue Kapitel folgen dem exakt; im Zweifel <code>manual/teil-a-anwenderhandbuch/05-karte.md</code> und <code>07-rohrzuordnung.md</code> als Vorlage lesen.</p><p><strong>Ansprache und Ton</strong></p><ul><li>Konsequent <strong>Sie-Form</strong>, Handlungsanweisungen im Imperativ: „Klicken Sie auf …“, „Geben Sie einen Suchbegriff ein.“</li><li>Geschlechtsneutral über Partizip/Doppelnennung: „Nutzende“, „Anwenderinnen und Anwender“, „Verwaltungsmitarbeitende“.</li><li>Sachlich, kein Marketing-Ton, keine Emojis, keine Ausrufezeichen.</li><li>Erklärt wird auch, was <strong>nicht</strong> geht und wo Nutzende hängenbleiben („Andernfalls gehen die Änderungen ohne Warnung verloren.“, „Wenn der Button nicht sichtbar ist, scrollen Sie im Feld nach unten.“).</li></ul><p><strong>Struktur</strong></p><ul><li>Keine Frontmatter in Kapiteldateien.</li><li>Überschriften nummeriert: <code># 6. Rohrverwaltung</code>, <code>## 6.1 Suchen und Filtern</code>, <code>### 6.3.1 Reiter „Eigenschaften“</code>. <code>####</code> bleibt unnummeriert.</li><li>Jedes Kapitel beginnt mit einem Absatz: Zweck des Bereichs + wie man hinkommt („Sie erreichen sie über die linke Navigation durch Klicken auf den Menüpunkt „Rohrverwaltung“.“), danach direkt ein Übersichts-Screenshot.</li><li>Aufzählungen für Optionen/Eigenschaften, <strong>nummerierte</strong> Listen nur für echte Schritt-für-Schritt-Abläufe.</li><li>Hinweise als normaler Absatz mit Präfix <code>Hinweis:</code>, <code>Wichtig:</code> oder <code>Wichtiger Hinweis:</code> – <strong>keine</strong> VitePress-Container (<code>::: tip</code>).</li><li>Querverweise als relative Links: <code>siehe Kapitel [Karte](./05-karte.md)</code>.</li><li>Platzhalter für noch offene Kapitel: <code>_Die Dokumentation zu diesem Kapitel ist noch in Arbeit._</code></li></ul><p><strong>Auszeichnung</strong></p><ul><li><code>**fett**</code> für Fachbegriffe und Konzepte beim ersten Auftreten (<strong>Rohrzuordnung</strong>, <strong>Transparenz</strong>, <strong>interaktive Legende</strong>) und für Zustände („Routing-Modus <strong>eingeschaltet</strong>“).</li><li>UI-Beschriftungen in typografischen Anführungszeichen: „Speichern“, „+ Rohr hinzufügen“, Reiter „Anhänge“. Beschriftungen wörtlich aus der App übernehmen – Referenz ist <code>local-app/frontend/messages/de.json</code>.</li><li>Abkürzungen mit Leerzeichen: „z. B.“, „ggf.“.</li></ul><h2 id="screenshots-und-videos" tabindex="-1">Screenshots und Videos <a class="header-anchor" href="#screenshots-und-videos" aria-label="Permalink to &quot;Screenshots und Videos&quot;">​</a></h2><p><strong>Technische Zielwerte</strong></p><table tabindex="0"><thead><tr><th></th><th>Wert</th></tr></thead><tbody><tr><td>Viewport</td><td>1792 × 1120, <code>deviceScaleFactor: 2</code> → Bild <strong>3584 × 2240</strong></td></tr><tr><td>Bildformat</td><td><code>.jpg</code>, Qualität ~85, Zieldateigröße &lt; 1,2 MB</td></tr><tr><td>Videoformat</td><td><code>.webm</code> (VP8), Ausschnitt der Oberfläche, ca. 1000 × 700</td></tr><tr><td>Modus</td><td>immer Hellmodus, Sprache <strong>DE</strong></td></tr><tr><td>Inhalt</td><td>nur der App-Viewport, kein Browser-Chrome; Bilder ohne Mauszeiger, Videos <strong>mit</strong></td></tr><tr><td>Daten</td><td>ausschließlich das Demoprojekt „Testprojekt“ – keine echten personenbezogenen Daten</td></tr></tbody></table><blockquote><p><code>playwright.config.ts</code> setzt diese Werte zentral. Kein <code>devices[...]</code>-Preset in die Projekt-Konfiguration spreaden – die Presets bringen eigene <code>viewport</code>- und <code>deviceScaleFactor</code>-Werte mit und überschreiben die Zielwerte still.</p></blockquote><p>Videos sind bewusst <strong>kein</strong> Vollbild der Oberfläche. Das Handbuch rendert sie mit der Breite des Textbereichs (rund 690 px); bei 1792 CSS-Pixeln Aufnahmebreite blieben von einer 16-px-Beschriftung der App keine 7 px übrig. Ein Ausschnitt von rund 1000 CSS-Pixeln Breite entspricht den bestehenden Videos (<code>map_attachment.webm</code> zeigte einen Bereich von etwa 924 × 638 CSS-Pixeln) und bleibt lesbar. Maßgeblich ist allein die <strong>Breite</strong> – die Höhe ändert den Maßstab im Handbuch nicht. Anders als Screenshots werden Videos außerdem in CSS-Pixeln aufgenommen: Chromiums Screencast liefert keine Gerätepunkte, der <code>deviceScaleFactor</code> von 2 wirkt dort nicht.</p><p><strong>Ablage und Benennung</strong></p><ul><li>Bilder: <code>public/images/manual/teil-a/&lt;name&gt;.jpg</code> (je Handbuchteil ein Ordner)</li><li>Videos: <code>public/videos/&lt;name&gt;.webm</code> (flach, ohne Teil-Unterordner)</li><li>Name = englisch, <code>snake_case</code>, Bereich zuerst, Detail danach: <code>login_</code>, <code>dashboard_</code>, <code>map_</code>, <code>conduit_</code>, <code>conduit_connection_</code> → <code>dashboard_trasse_hover.jpg</code>, <code>map_legend_actions.jpg</code>, <code>conduit_search_columns.jpg</code></li><li>Ausschnittvergrößerungen bekommen das Suffix <code>_detail</code> (<code>login_start_detail.jpg</code>, <code>map_adress_detail.jpg</code>).</li></ul><p><strong>Bildsprache – vier Muster, die konsistent wiederkehren</strong></p><ol><li><strong>Übersichtsbild, unbearbeitet</strong> – der ganze App-Viewport, ohne Markierung. Steht am Kapitelanfang (<code>dashboard.jpg</code>, <code>map.jpg</code>, <code>conduit.jpg</code>).</li><li><strong>Dim + Spotlight</strong> – die gesamte Oberfläche wird abgedunkelt, nur der beschriebene Bereich bleibt in voller Helligkeit und bekommt eine weiße, abgerundete Kontur. Das Standardmittel für „Wo finde ich X?“ (<code>dashboard_project.jpg</code>, <code>conduit_excel.jpg</code>, <code>map_selected_object.jpg</code>). Der Schleier ist <strong><code>rgba(0, 0, 0, 0.5)</code></strong> – schwarz bei 50 %, nicht grau. An den Altbildern nachgemessen: jeder abgedunkelte Bildpunkt hat exakt den halben Wert des unabgedunkelten Bildes (255 → 128, 220 → 110). Ein grauer oder schwächerer Schleier wirkt neben den bestehenden Bildern deutlich zu hell; <code>spotlight()</code> in <code>playwright/manual-shots.ts</code> setzt diesen Wert.</li><li><strong>Handgezeichnete Markierung in Marken-Grün</strong> (<code>#11ba81</code>) – geschwungene Ellipsen um Elemente, gebogene Pfeile und handschriftlich wirkende Labels. Für Orientierungsbilder mit mehreren Beschriftungen gleichzeitig (<code>login_navigation.jpg</code>) und zum Hervorheben eines Kartenobjekts.</li><li><strong>Composite-Raster</strong> – 2 × 2 Einzelbilder mit weißen Fugen, jeder Schritt unten rechts mit einer großen grünen Ziffer nummeriert; die Ziffern entsprechen den Schritten der nummerierten Liste im Text (<code>map_search_flow.jpg</code>, <code>map_legend_actions.jpg</code>).</li></ol><p>Muster 2 und 3 werden kombiniert (abdunkeln + Ellipse + Pfeil). Videos sind kurze, ungeschnittene Interaktionsaufnahmen ohne Ton, Text oder Markierung – mit sichtbarem Mauszeiger, weil Zustände wie „Schaltflächen erscheinen beim Zeigen“ sonst grundlos wirken (<code>mauszeigerAn()</code> in <code>playwright/manual-videos.ts</code> legt einen nachgebildeten Zeiger in die Seite; Playwright zeichnet den echten nicht mit). Animationen bleiben an, <code>animationenAus()</code> gilt nur für Standbilder.</p><p><strong>Einbindung in Markdown</strong></p><ul><li>Bild steht <strong>nach</strong> dem erklärenden Absatz, nie davor.</li><li>Alt-Text ist eine deutsche Beschreibung, die Ansicht, Hervorhebung und Position nennt: <code>![Screenshot Karte mit Hervorhebung der Legende oben rechts](/images/manual/teil-a/map_legend.jpg)</code></li><li>Bildklassen (siehe <code>.vitepress/theme/custom.css</code>): Standard 512 px mit grünem 1-px-Rahmen, <code>{.big}</code> = 800 px, <code>{.small}</code> = 300 px, <code>{.no-border}</code> ohne Rahmen.</li><li>Bildpaar (Vollbild + Detail) nebeneinander: zwei <code>![]()</code>-Zeilen direkt untereinander, danach eine eigene Zeile mit <code>{.img-row}</code>.</li><li>Videos ohne Alt-Text über <code>markdown-it-html5-media</code>: <code>![](/videos/conduit_connection_mapFind.webm)</code></li><li>Klick auf Bilder öffnet eine Lightbox (<code>vitepress-plugin-lightbox</code>) – Details dürfen deshalb im 512-px-Rendering klein sein, müssen im Original aber lesbar sein.</li></ul><h2 id="lokale-qonnectra-instanz-fur-reproduzierbare-aufnahmen" tabindex="-1">Lokale Qonnectra-Instanz für reproduzierbare Aufnahmen <a class="header-anchor" href="#lokale-qonnectra-instanz-fur-reproduzierbare-aufnahmen" aria-label="Permalink to &quot;Lokale Qonnectra-Instanz für reproduzierbare Aufnahmen&quot;">​</a></h2><p>Ziel: Screenshots und Videos entstehen künftig als Playwright-Testfälle gegen die lokale Instanz, damit sie bei Änderungen der App neu erzeugt werden können.</p><ul><li><code>scripts/setup-local-qonnectra.sh</code> klont die App nach <code>local-app/</code> und startet sie über die <strong>Produktions</strong>-Compose. Idempotent, beliebig oft ausführbar.</li><li>Erreichbar unter <code>https://app.qonnectra.localhost</code> (Admin: <code>https://admin.qonnectra.localhost/admin</code>, API: <code>https://api.qonnectra.localhost</code>).</li><li>Zwei Konten, Zugangsdaten in <code>local-app/deployment/.env</code>, beim ersten Lauf zufällig erzeugt. <strong>Nie in Doku, Tests, Skripte oder Commits schreiben</strong> – immer über <code>process.env</code> bzw. die <code>.env</code>-Datei einlesen. <ul><li><code>APP_USER_USERNAME</code> / <code>APP_USER_PASSWORD</code> – Konto <strong>ohne</strong> Administrationsrechte, Gruppe aus <code>APP_USER_GROUP</code> (Voreinstellung <code>Editor</code>: alle Fachdaten bearbeitbar, kein Zugriff auf <code>/admin/*</code>). <strong>Der Standard für alle Aufnahmen</strong> – Teil A beschreibt die Sicht normaler Nutzender. Das Setup legt es bei jedem Lauf neu an bzw. aktualisiert es.</li><li><code>DJANGO_SUPERUSER_USERNAME</code> / <code>DJANGO_SUPERUSER_PASSWORD</code> – Django-Superuser für die Administration. Nur für Bilder von <code>/admin/*</code> benutzen: er umgeht jede Rechteprüfung und sieht zusätzlich den Menüpunkt „Logs“.</li></ul></li><li><code>PUBLIC_DOCUMENTATION_URL</code> in <code>.env</code> ist der Hilfe-Link, den die App in Kopfzeile und Navigationsleiste zeigt; das Setup setzt ihn bei jedem Lauf auf <code>https://qonnectra.de/</code> (überschreibbar via <code>QONNECTRA_DOCUMENTATION_URL</code>). Ist die Variable leer, blendet die App den Link aus und er fehlt im Bild.</li><li>HTTPS läuft über eine lokale Dev-CA; einmalig <code>scripts/install-local-ca.sh</code> ausführen, alternativ in Playwright <code>ignoreHTTPSErrors: true</code> setzen.</li><li>Demodaten: Projekt <strong>„Testprojekt“</strong> aus <code>scripts/qonnectra-demo-data/testprojekt-export.json</code>, wird beim Setup automatisch importiert. Nach dem Login oben links auswählen.</li><li><code>local-app/</code> ist gitignored (Fremd-Checkout) – nie committen und nur über das Setup-Skript verändern.</li></ul><p><strong>Playwright-Setup im Doku-Repo</strong></p><p>Alle Läufe gehen ausschließlich gegen die lokale Instanz. Es gibt keine konfigurierbare Zieladresse und kein <code>.env</code> im Repo-Root mehr – <code>GEODOCK_URL</code> wird nicht mehr gelesen.</p><ul><li><p><code>playwright/local-app.ts</code> ist die einzige Quelle für Adresse und Zugangsdaten und liest <code>local-app/deployment/.env</code> (<code>APP_DOMAIN</code>, <code>API_DOMAIN</code>, <code>APP_USER_*</code>, <code>DJANGO_SUPERUSER_*</code>). Zugangsdaten nur über <code>localApp()</code> beziehen, nie in Specs, Ausgaben oder Commits schreiben.</p></li><li><p>Angemeldet wird sich standardmäßig mit dem Konto <strong>ohne</strong> Administrationsrechte. <code>QONNECTRA_LOGIN=admin pnpm test:e2e</code> schaltet auf den Superuser um – nur für Bereiche, die normalen Nutzenden verborgen bleiben. Bilder aus einem Admin-Lauf zeigen sonst eine Oberfläche, die es für die Zielgruppe von Teil A nicht gibt.</p></li><li><p><code>playwright/auth.setup.ts</code> läuft als Setup-Projekt automatisch vor jedem Spec: prüft die Erreichbarkeit (mit Hinweis auf <code>scripts/setup-local-qonnectra.sh</code>, falls der Stack steht), meldet sich per <code>POST /api/v1/auth/login/</code> an und schreibt <code>auth-state.json</code>. <code>pnpm test:e2e:setup</code> führt nur diesen Schritt aus.</p></li><li><p><code>auth-state.json</code> ist <strong>nicht</strong> wiederverwendbar und wird pro Lauf neu erzeugt: das Access-Token lebt 15 Minuten, und das Backend rotiert Refresh-Tokens mit Blacklist (<code>ROTATE_REFRESH_TOKENS</code> + <code>BLACKLIST_AFTER_ROTATION</code>).</p></li><li><p>Das Setup legt den Zustand fest, an dem die Bilder hängen: Cookie <code>selected-project=2</code> („Testprojekt“; ein UI-Login würde <code>1</code> = „Default“ schreiben) sowie im <code>localStorage</code> <code>PARAGLIDE_LOCALE=de</code>, <code>mode=light</code>, <code>basemapTheme</code>, <code>mapCenter</code> und <code>mapZoom</code>. Die Karte hat kein Auto-Fit – ohne gesetzte <code>mapCenter</code>/<code>mapZoom</code> (EPSG:3857) startet sie bei Zoom 2 im Atlantik.</p></li><li><p>Ein Spec pro Handbuchkapitel: <code>tests/&lt;NN&gt;-&lt;kapitel-slug&gt;.spec.ts</code> mit Kommentar, auf welches Kapitel es sich bezieht (siehe <code>tests/05-karte.spec.ts</code>). Videos eines Kapitels liegen daneben in <code>tests/&lt;NN&gt;-&lt;kapitel-slug&gt;-video.spec.ts</code>; eine eigene Datei ist Pflicht, weil <code>test.use({ video: … })</code> nur auf Dateiebene erlaubt ist („forces a new worker“ in einer <code>test.describe</code>-Gruppe).</p></li><li><p>Ausgabe nach <code>tests/screenshots/&lt;kapitel-slug&gt;/&lt;bildname&gt;.png</code> über <code>shotPath()</code> bzw. <code>tests/videos/&lt;kapitel-slug&gt;/&lt;name&gt;.webm</code> über <code>videoPfad()</code>. <code>tests/screenshots/</code>, <code>tests/videos/</code>, <code>test-results/</code>, <code>playwright-report/</code> und <code>auth-state.json</code> sind gitignored – das sind Rohaufnahmen, nicht die Dateien des Handbuchs.</p></li><li><p><code>pnpm screenshots:publish</code> (<code>scripts/publish-screenshots.sh</code>) übernimmt sie nach <code>public/images/manual/…</code> bzw. <code>public/videos/…</code> und wandelt Bilder dabei in JPEG (Qualität 85, Qualität wird gesenkt, bis die Datei unter 1,2 MB liegt); Videos werden nur kopiert, Zuschnitt und Kodierung erledigt die Spec. Das Ziel kommt aus dem Handbuch selbst: das Skript sucht in <code>manual/</code> die Einbindung <code>/images/manual/&lt;teil&gt;/&lt;name&gt;.jpg</code> bzw. <code>/videos/&lt;name&gt;.webm</code>. Aufnahmen ohne Einbindung werden übersprungen, damit nichts im falschen Ordner landet. <code>--dry-run</code> zeigt vorher, was neu angelegt und was ersetzt würde, <code>--videos</code> und <code>--bilder</code> schränken auf eine Art ein.</p></li><li><p>Nur Muster 1 und 2 gehen komplett automatisch durch. Bilder mit handgezeichneten Markierungen (Muster 3) werden nach dem Übernehmen nachbearbeitet – <code>--dry-run</code> vorher ansehen, sonst überschreibt der Lauf die Handarbeit mit einer Rohaufnahme. Wer nur ein Video erneuert, nimmt <code>--videos</code>.</p></li><li><p>Kapitel 3 braucht als einziges auch den abgemeldeten Zustand: die Bilder der Anmeldeseite stehen in einem <code>test.describe</code>-Block mit <code>test.use({ storageState: { cookies: [], origins: [] } })</code>, die Bilder der Oberfläche daneben im normalen angemeldeten Zustand. Angemeldete Aufrufe von <code>/login</code> leitet die App auf <code>/map</code> um.</p></li><li><p><code>workers: 1</code> und <code>fullyParallel: false</code> sind Absicht: alle Specs teilen sich eine Instanz samt Projektauswahl und Kartenposition.</p></li><li><p>Determinismus-Helfer in <code>playwright/manual-shots.ts</code>: <code>animationenAus()</code> (Übergänge und Text-Cursor aus), <code>zeigerWeg()</code> (keine Hover-Zustände im Bild), <code>spotlight()</code> für Muster 2 und <code>composite2x2()</code> für Muster 4. Das Raster wird im Browser montiert, das Repo braucht dafür keine Bildbibliothek. Muster 3 (handgezeichnete Ellipsen/Pfeile) bleibt Nachbearbeitung. <code>spotlight()</code> legt ein SVG mit Aussparung über die Seite und verändert das Zielelement nicht. Der naheliegende Weg über <code>box-shadow: 0 0 0 9999px</code> am Element selbst scheitert hier zweifach: der Schleier wird am nächsten Vorfahren mit <code>overflow: hidden</code> abgeschnitten, und macht man die Vorfahren durchlässig, verliert die Karte (OpenLayers) beim Reflow ihren Canvas-Inhalt.</p></li><li><p>Video-Helfer in <code>playwright/manual-videos.ts</code>: <code>mauszeigerAn()</code> legt einen nachgebildeten Mauszeiger in die Seite, <code>zeigeAuf()</code>, <code>klicke()</code>, <code>ziehe()</code> und <code>tippe()</code> führen ihn in Handgeschwindigkeit, <code>videoNachbearbeiten()</code> schneidet Seitenaufbau und Bildrand weg. Fallstricke, die dort schon gelöst sind und beim Nachbauen sofort wieder auftauchen:</p><ul><li>Der Zeiger darf <strong>keine</strong> eigene Compositor-Ebene bekommen (also <code>left</code>/<code>top</code> statt <code>transform</code>, kein <code>will-change</code>). Sonst zeichnet Chromium ihn weiter aktuell, während das Rastern des übrigen Inhalts hinterherhinkt – beim Breiterziehen der Info-Box lief der Zeiger der Kante um gut 180 px voraus.</li><li>Der Zeiger muss auf <code>pointermove</code> <strong>und</strong> <code>mousemove</code> hören: Der Griff der Info-Box ruft in seinem <code>pointerdown</code> <code>preventDefault()</code> auf, danach liefert Chromium für diesen Zeiger keine <code>mouse</code>-Ereignisse mehr.</li><li>Beim Ziehen rund 90 ms je Schritt lassen. An der Breite der Info-Box hängt die Karte, OpenLayers zeichnet bei jeder Änderung neu (gemessen ~70 ms); dichtere Ereignisse stauen sich sichtbar auf.</li><li>Geschnitten wird mit dem ffmpeg, das <code>playwright install</code> ohnehin mitbringt (<code>ffmpegPfad()</code>) – kein zusätzliches Werkzeug auf dem Rechner. <code>-ss</code> muss <strong>hinter</strong> <code>-i</code> stehen; davor spult ffmpeg nur bis zum letzten Schlüsselbild, und die sitzen in Playwrights Aufnahme weit auseinander.</li><li>Legt eine Aufnahme Daten an (z. B. einen Anhang), räumt die Spec sie über die API wieder weg – und zwar mit <code>superuserZugang()</code>. Die Gruppe „Editor“ des Aufnahmekontos hat auf alle Fachmodelle nur die Stufe „edit“ und darf kein DELETE (<code>RoleBasedPermission</code>); die API antwortet mit 403.</li></ul></li><li><p>Composite-Raster sind montiert und deshalb <strong>nicht</strong> 3584 × 2240, sondern 2656 px breit bei einer Höhe, die sich aus dem Kartenausschnitt der Kacheln ergibt (zuletzt 2656 × 1854). Das Seitenverhältnis der Montage lässt sich nicht auf beide Zielmaße gleichzeitig bringen; im Handbuch werden die Bilder ohnehin auf 512 px gerendert.</p></li><li><p>Die Kartenkacheln erzeugt <code>scripts/setup-local-qonnectra.sh</code> einmalig per Planetiler (Region <code>schleswig-holstein</code>, dort liegt das Testprojekt) und legt sie unter <code>~/.local/share/qonnectra-local-tiles/</code> ab – außerhalb von <code>local-app/</code>, damit <code>--reset</code> sie nicht wegwirft. Der <code>tileserver</code> bekommt sie als harte Verknüpfung unter <code>local-app/deployment/tiles/germany.mbtiles</code> (ein Bind-Mount nur für die Datei scheitert, weil Docker den Mountpoint im read-only gemounteten <code>/data</code> nicht anlegen kann). Kartenbilder zeigen damit die echte Vektor-Basiskarte im Hellmodus. Fehlt die <code>.mbtiles</code> (Lauf mit <code>--skip-tiles</code>, kein Java), läuft der <code>tileserver</code> in einer Restart-Schleife und die Karte fällt auf OSM-Rasterkacheln zurück.</p></li><li><p>Antwortet die API mit <strong>502</strong>, obwohl der Backend-Container läuft: nach einem Neustart des Backends hat <code>nginx</code> dessen alte Container-IP zwischengespeichert (nginx-Log: „Host is unreachable“) und löst sie nicht neu auf. Behebt sich mit <code>docker restart qonnectra_nginx_prod</code>. Das Setup-Projekt wartet eine Minute auf 5xx-Antworten, weil ein kalt gestarteter Stack kurz mit 502 antwortet.</p></li><li><p>Die Zahl der Canvas-Elemente in der Karte hängt vom Tileserver ab: mit Vektorkacheln legt OpenLayers zwei an, im OSM-Rasterfallback eines. Deshalb <code>page.locator(&#39;div.map canvas&#39;).first()</code> verwenden.</p></li><li><p>Kartenposition immer per <code>page.addInitScript()</code> setzen, nicht per „laden, <code>localStorage</code> setzen, neu laden“. Die App schreibt <code>mapCenter</code> und <code>mapZoom</code> bei jedem <code>moveend</code> zurück; landet das zwischen Setzen und Neuladen, ist der Seed weg und die Karte startet in der Übersicht. Tests, die auf eine bestimmte Stelle klicken, treffen dann nichts und die Info-Box öffnet nicht (Symptom: <code>#drawer-title</code> nicht gefunden).</p></li><li><p>Der Basiskarten-Layer ist von der Objektauswahl unabhängig: <code>getClickedFeatures</code> filtert per <code>layerFilter</code> auf Trasse, Adresse, Netzknoten und Gebiet (<code>MapInteractionManager.svelte.ts</code>). Ob der Tileserver läuft, hat auf Klicks also keinen Einfluss.</p></li><li><p>Ein Kartenobjekt per Klick auszuwählen ist nicht ohne Weiteres reproduzierbar: Eine Trassenlinie ist nur wenige Pixel breit, und darunter liegt das Projektgebiet, dessen Fläche das ganze Netz überdeckt. Dieselbe Stelle liefert je nach Lauf verschiedene Trassen oder das Gebiet. Wiederholtes Klicken hilft nicht (es ist kein Kachel-Rennen) – für einen deterministischen Treffer den Layer „Gebiet&quot; vor dem Klick ausblenden. Wieder einschalten geht danach nicht, weil die geöffnete Info-Box die Legende verdeckt.</p></li><li><p>Die Fensterhöhe von 1120 px ist an der Navigationsleiste bemessen: mit allen aufgeklappten Gruppen braucht sie 1093 px Inhalt (gemessen), bei den früheren 800 px lag die Gruppe „System&quot; unter dem sichtbaren Bereich und musste erst herangescrollt werden. Wächst die Leiste über 1120 px hinaus, den Viewport in <code>playwright.config.ts</code> erhöhen und <strong>nicht</strong> Gruppen einklappen; das wäre ein Zustand, den Nutzende erst selbst herstellen müssen. Scroll-Container der Leiste ist ihr Raster, greifbar über <code>div[class*=&quot;grid-rows-[auto_1fr_auto]&quot;]</code> (<code>SideBar.svelte</code>).</p></li></ul><p><strong>Die App (Kontext für Selektoren und Routen)</strong></p><p>SvelteKit + Skeleton. Die Navigationsleiste ist nach Gruppen sortiert; die Beschriftungen sind kurz und erst mit der Gruppe eindeutig (Gruppe „Rohr“ → „Verwaltung“ = Rohrverwaltung). Routen und Beschriftungen:</p><table tabindex="0"><thead><tr><th>Gruppe</th><th>Route → Beschriftung</th></tr></thead><tbody><tr><td>„Info“</td><td><code>/dashboard</code> „Dashboard“, <code>/map</code> „Karte“</td></tr><tr><td>„Funktionen“</td><td><code>/fault-simulation</code> „Störungsanalyse“, <code>/post-compaction</code> „Nachverdichtung“, <code>/pipeline-records</code> „Leitungsauskunft“, <code>/valuation</code> „Wertermittlung“</td></tr><tr><td>„Rohr“</td><td><code>/conduit</code> „Verwaltung“, <code>/trench</code> „Zuordnung“, <code>/pipe-branch</code> „Verzweigung“, <code>/house-connections</code> „Mikrorohre“</td></tr><tr><td>„Kabel“</td><td><code>/network-schema</code> „Netzschema“, <code>/trace</code> „Faserweg“</td></tr><tr><td>„Gebäude“</td><td><code>/address</code> „Adressen“</td></tr><tr><td>„System“</td><td><code>/admin/logs</code> „Logs“, <code>/settings</code> „Einstellungen“</td></tr></tbody></table><p>Dazu <code>/login</code> ohne Navigationsleiste. Welche Einträge erscheinen, hängt an den Rechten (<code>canAccessRoute</code>); als Superuser sind alle sichtbar. Mit dem Standardkonto der Aufnahmen (Gruppe „Editor“) fehlt in der Gruppe „System“ der Eintrag „Logs“ – die Gruppe besteht dort nur aus „Einstellungen“. <code>/admin/*</code> ist der einzige gesperrte Pfad; alles ohne eigenen <code>RoutePermission</code>-Eintrag gilt als erlaubt. Navigationsdefinition: <code>local-app/frontend/src/lib/config/navLinks.ts</code>, UI-Texte: <code>local-app/frontend/messages/de.json</code>.</p><h2 id="subagents" tabindex="-1">Subagents <a class="header-anchor" href="#subagents" aria-label="Permalink to &quot;Subagents&quot;">​</a></h2><ul><li><code>handbuch-autor</code> – neue oder erweiterte Handbuchkapitel im obigen Stil</li><li><code>screenshot-automat</code> – Playwright-Specs für Screenshots/Videos schreiben und ausführen</li><li><code>handbuch-review</code> – Stil-, Konsistenz- und Rechtschreibprüfung vor dem Commit</li></ul>`,44)])])}const g=n(r,[["render",s]]);export{p as __pageData,g as default};
