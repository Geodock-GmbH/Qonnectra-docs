import{_ as t,c as o,o as a,ah as s}from"./chunks/framework.oAolcYhj.js";const g=JSON.parse('{"title":"CLAUDE.md","description":"","frontmatter":{},"headers":[],"relativePath":"CLAUDE.md","filePath":"CLAUDE.md"}'),n={name:"CLAUDE.md"};function i(d,e,r,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="claude-md" tabindex="-1">CLAUDE.md <a class="header-anchor" href="#claude-md" aria-label="Permalink to &quot;CLAUDE.md&quot;">​</a></h1><p>Guide for Claude Code in this repository.</p><p><strong>Language: everything is English</strong> – identifiers, comments, script and console output, CLI flags, commit messages and this file. German survives in exactly three places, each for a reason:</p><ol><li><strong>The manual itself</strong> – <code>manual/</code> prose, alt texts, chapter file names (they are public URLs).</li><li><strong>Literals quoted from the German app</strong> – Playwright selectors (<code>getByRole(&#39;tab&#39;, { name: &#39;Trasse&#39; })</code>), UI labels cited in comments, demo data values (<code>&quot;Testprojekt&quot;</code>, <code>&quot;Hausanschluss&quot;</code>).</li><li><strong>Test titles</strong> – they mirror the manual&#39;s chapter headings 1:1, so a failing capture points straight at the chapter to fix.</li></ol><p>Everything else is English. When in doubt, English.</p><h2 id="glossary" tabindex="-1">Glossary <a class="header-anchor" href="#glossary" aria-label="Permalink to &quot;Glossary&quot;">​</a></h2><p>Derived from the already-English image names, so that code and <code>public/images/</code> agree:</p><table tabindex="0"><thead><tr><th>German</th><th>English</th><th>German</th><th>English</th></tr></thead><tbody><tr><td>Trasse</td><td>trench</td><td>Aufnahme</td><td>capture</td></tr><tr><td>Rohr</td><td>conduit</td><td>Ausschnitt</td><td>crop</td></tr><tr><td>Netzknoten</td><td>node</td><td>Schleier</td><td>scrim</td></tr><tr><td>Gebiet</td><td>area</td><td>Kontur</td><td>outline</td></tr><tr><td>Adresse</td><td>address</td><td>Zeiger</td><td>cursor</td></tr><tr><td>Gewährleistung</td><td>warranty</td><td>Frist</td><td>deadline</td></tr><tr><td>Anhang</td><td>attachment</td><td>Vorlauf</td><td>lead-in</td></tr><tr><td>Reiter</td><td>tab</td><td>Verzögerung</td><td>delay</td></tr><tr><td>Kapitel</td><td>chapter</td><td>übernommen</td><td>published</td></tr><tr><td>Legende</td><td>legend</td><td>übersprungen</td><td>skipped</td></tr><tr><td>Diagramm</td><td>chart</td><td>Balken</td><td>bar</td></tr></tbody></table><p>One word, two meanings: <strong>Karte</strong> is the map in <code>05-karte.spec.ts</code> (<code>openMap()</code>), but a dashboard card in <code>04-dashboard.spec.ts</code> (<code>card()</code>). Never translate it globally.</p><h2 id="what-this-repo-is" tabindex="-1">What this repo is <a class="header-anchor" href="#what-this-repo-is" aria-label="Permalink to &quot;What this repo is&quot;">​</a></h2><p>VitePress site for <strong>Qonnectra</strong> (open-source network documentation for municipal infrastructure, Geodock GmbH &amp; plan[neo] GmbH, AGPL-3.0). Two content strands:</p><ul><li><strong>Landing page</strong>: <code>index.md</code>, <code>services/</code>, <code>contact/</code>, <code>imprint/</code>, <code>privacy/</code></li><li><strong>Manual</strong>: <code>manual/</code> – the focus of ongoing work</li></ul><p>The manual is split into three parts (target audiences see <code>manual/index.md</code>):</p><table tabindex="0"><thead><tr><th>Directory</th><th>Part</th><th>Chapters</th></tr></thead><tbody><tr><td><code>manual/index.md</code></td><td>Introduction</td><td>1</td></tr><tr><td><code>manual/teil-a-anwenderhandbuch/</code></td><td>A – User manual (web application, no GIS knowledge)</td><td>2–7, reserved 8–13</td></tr><tr><td><code>manual/teil-b-betrieb-admin-qgis/</code></td><td>B – Operations, administration, QGIS</td><td>14, reserved 15–16</td></tr><tr><td><code>manual/teil-c-entwickler-systemdokumentation/</code></td><td>C – Developer and system documentation</td><td>17 ff.</td></tr></tbody></table><p>The gaps in the numbering are deliberate – new chapters fill them instead of shifting existing numbers. The chapter number in the H1 <strong>and</strong> in the file name prefix have to match (<code>06-rohrverwaltung.md</code> → <code># 6. Rohrverwaltung</code>); the sidebar is generated from file name order + H1 (<code>vitepress-sidebar</code>).</p><h2 id="commands" tabindex="-1">Commands <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;Commands&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # http://localhost:5173</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # BASE_PATH=&quot;/Qonnectra-docs/&quot; in CI</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> lint:spelling</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # cspell (en, en-GB, de) – has to be green before every commit</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e:setup</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # write the login state to auth-state.json</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test:e2e</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # Playwright specs in tests/</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/setup-local-qonnectra.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # build/start the local Qonnectra instance</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/setup-local-qonnectra.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --reset</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # discard data + secrets, rebuild</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scripts/install-local-ca.sh</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # import the dev CA once per machine</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>New German technical terms cspell does not know go into <code>.cspell.json</code> under <code>words</code>, sorted alphabetically – not suppressed with an inline comment.</p><h2 id="writing-style-of-the-manual" tabindex="-1">Writing style of the manual <a class="header-anchor" href="#writing-style-of-the-manual" aria-label="Permalink to &quot;Writing style of the manual&quot;">​</a></h2><p>Binding, derived from the existing chapters. New chapters follow it exactly; when in doubt read <code>manual/teil-a-anwenderhandbuch/05-karte.md</code> and <code>07-rohrzuordnung.md</code> as templates. The manual is written in German, so the rules below quote German.</p><p><strong>Form of address and tone</strong></p><ul><li>Consistently <strong>Sie-Form</strong>, instructions in the imperative: „Klicken Sie auf …“, „Geben Sie einen Suchbegriff ein.“</li><li>Gender-neutral through participles/double naming: „Nutzende“, „Anwenderinnen und Anwender“, „Verwaltungsmitarbeitende“.</li><li>Factual, no marketing tone, no emoji, no exclamation marks.</li><li>Explain what does <strong>not</strong> work and where users get stuck as well („Andernfalls gehen die Änderungen ohne Warnung verloren.“, „Wenn der Button nicht sichtbar ist, scrollen Sie im Feld nach unten.“).</li></ul><p><strong>Structure</strong></p><ul><li>No frontmatter in chapter files.</li><li>Numbered headings: <code># 6. Rohrverwaltung</code>, <code>## 6.1 Suchen und Filtern</code>, <code>### 6.3.1 Reiter „Eigenschaften“</code>. <code>####</code> stays unnumbered.</li><li>Every chapter starts with a paragraph: purpose of the area + how to get there („Sie erreichen sie über die linke Navigation durch Klicken auf den Menüpunkt „Rohrverwaltung“.“), followed directly by an overview screenshot.</li><li>Bullet lists for options/properties, <strong>numbered</strong> lists only for genuine step-by-step procedures.</li><li>Notes as a normal paragraph with the prefix <code>Hinweis:</code>, <code>Wichtig:</code> or <code>Wichtiger Hinweis:</code> – <strong>no</strong> VitePress containers (<code>::: tip</code>).</li><li>Cross-references as relative links: <code>siehe Kapitel [Karte](./05-karte.md)</code>.</li><li>Placeholder for chapters still to be written: <code>_Die Dokumentation zu diesem Kapitel ist noch in Arbeit._</code></li></ul><p><strong>Markup</strong></p><ul><li><code>**bold**</code> for technical terms and concepts on first appearance (<strong>Rohrzuordnung</strong>, <strong>Transparenz</strong>, <strong>interaktive Legende</strong>) and for states („Routing-Modus <strong>eingeschaltet</strong>“).</li><li>UI labels in typographic quotation marks: „Speichern“, „+ Rohr hinzufügen“, Reiter „Anhänge“. Take labels verbatim from the app – the reference is <code>local-app/frontend/messages/de.json</code>.</li><li>Abbreviations with a space: „z. B.“, „ggf.“.</li></ul><h2 id="screenshots-and-videos" tabindex="-1">Screenshots and videos <a class="header-anchor" href="#screenshots-and-videos" aria-label="Permalink to &quot;Screenshots and videos&quot;">​</a></h2><p><strong>Technical target values</strong></p><table tabindex="0"><thead><tr><th></th><th>Value</th></tr></thead><tbody><tr><td>Viewport</td><td>1792 × 1120, <code>deviceScaleFactor: 2</code> → image <strong>3584 × 2240</strong></td></tr><tr><td>Image format</td><td><code>.jpg</code>, quality ~85, target file size &lt; 1.2 MB</td></tr><tr><td>Video format</td><td><code>.webm</code> (VP8), crop of the interface, approx. 1000 × 700</td></tr><tr><td>Mode</td><td>always light mode, language <strong>DE</strong></td></tr><tr><td>Content</td><td>only the app viewport, no browser chrome; images without a mouse cursor, videos <strong>with</strong> one</td></tr><tr><td>Data</td><td>exclusively the demo project „Testprojekt“ – no real personal data</td></tr></tbody></table><blockquote><p><code>playwright.config.ts</code> sets these values centrally. Do not spread a <code>devices[...]</code> preset into the project configuration – the presets bring their own <code>viewport</code> and <code>deviceScaleFactor</code> values and override the target values silently.</p></blockquote><p>Videos are deliberately <strong>not</strong> a full shot of the interface. The manual renders them at the width of the text column (around 690 px); at a recording width of 1792 CSS pixels, a 16 px label in the app would be left with less than 7 px. A crop of around 1000 CSS pixels wide matches the existing videos (<code>map_attachment.webm</code> showed an area of roughly 924 × 638 CSS pixels) and stays legible. Only the <strong>width</strong> matters – the height does not change the scale in the manual. Unlike screenshots, videos are also recorded in CSS pixels: Chromium&#39;s screencast delivers no device pixels, so the <code>deviceScaleFactor</code> of 2 has no effect there.</p><p><strong>Location and naming</strong></p><ul><li>Images: <code>public/images/manual/teil-a/&lt;name&gt;.jpg</code> (one folder per manual part)</li><li>Videos: <code>public/videos/&lt;name&gt;.webm</code> (flat, no part subfolder)</li><li>Name = English, <code>snake_case</code>, area first, detail second: <code>login_</code>, <code>dashboard_</code>, <code>map_</code>, <code>conduit_</code>, <code>conduit_connection_</code> → <code>dashboard_trench_hover.jpg</code>, <code>map_legend_actions.jpg</code>, <code>conduit_search_columns.jpg</code></li><li>Detail crops get the suffix <code>_detail</code> (<code>login_start_detail.jpg</code>, <code>map_address_detail.jpg</code>).</li></ul><p><strong>Visual language – four patterns that recur consistently</strong></p><ol><li><strong>Overview image, unedited</strong> – the whole app viewport, without annotation. Sits at the start of a chapter (<code>dashboard.jpg</code>, <code>map.jpg</code>, <code>conduit.jpg</code>).</li><li><strong>Dim + spotlight</strong> – the entire interface is dimmed, only the described area stays at full brightness and gets a white, rounded outline. The standard device for &quot;where do I find X?&quot; (<code>dashboard_project.jpg</code>, <code>conduit_excel.jpg</code>, <code>map_selected_object.jpg</code>). The scrim is <strong><code>rgba(0, 0, 0, 0.5)</code></strong> – black at 50 %, not grey. Measured on the old images: every dimmed pixel has exactly half the value of the undimmed image (255 → 128, 220 → 110). A grey or weaker scrim looks clearly too light next to the existing images; <code>spotlight()</code> in <code>playwright/manual-shots.ts</code> sets this value. Several places may be exposed at once when they belong together – <code>map_selected_object.jpg</code> shows the selected map object and the info box with its values, everything in between stays dimmed. For map objects the cut-out is an ellipse aligned to the line, not a rectangle.</li><li><strong>Hand-drawn annotation in brand green</strong> (<code>#11ba81</code>) – sweeping ellipses around elements, curved arrows and handwritten-looking labels. For orientation images with several labels at once (<code>login_navigation.jpg</code>).</li><li><strong>Composite grid</strong> – 2 × 2 individual images with white gutters, each step numbered with a large green digit in the bottom right; the digits correspond to the steps of the numbered list in the text (<code>map_search_flow.jpg</code>, <code>map_legend_actions.jpg</code>).</li></ol><p>Patterns 2 and 3 are combined (dim + ellipse + arrow). Videos are short, uncut interaction recordings without sound, text or annotation – with a visible mouse cursor, because states like &quot;buttons appear on hover&quot; would otherwise look unmotivated (<code>showCursor()</code> in <code>playwright/manual-videos.ts</code> places a replica cursor into the page; Playwright does not record the real one). Animations stay on; <code>disableAnimations()</code> applies to still images only.</p><p><strong>Embedding in Markdown</strong></p><ul><li>The image goes <strong>after</strong> the explaining paragraph, never before it.</li><li>The alt text is a German description naming the view, the highlight and the position: <code>![Screenshot Karte mit Hervorhebung der Legende oben rechts](/images/manual/teil-a/map_legend.jpg)</code></li><li>Image classes (see <code>.vitepress/theme/custom.css</code>): default 512 px with a green 1 px border, <code>{.big}</code> = 800 px, <code>{.small}</code> = 300 px, <code>{.no-border}</code> without a border.</li><li>Image pair (full shot + detail) side by side: two <code>![]()</code> lines directly below one another, then a line of its own with <code>{.img-row}</code>.</li><li>Videos without alt text through <code>markdown-it-html5-media</code>: <code>![](/videos/conduit_connection_mapFind.webm)</code></li><li>Clicking an image opens a lightbox (<code>vitepress-plugin-lightbox</code>) – details may therefore be small in the 512 px rendering, but have to be legible in the original.</li></ul><h2 id="local-qonnectra-instance-for-reproducible-captures" tabindex="-1">Local Qonnectra instance for reproducible captures <a class="header-anchor" href="#local-qonnectra-instance-for-reproducible-captures" aria-label="Permalink to &quot;Local Qonnectra instance for reproducible captures&quot;">​</a></h2><p>Goal: screenshots and videos are produced as Playwright test cases against the local instance, so that they can be regenerated when the app changes.</p><ul><li><code>scripts/setup-local-qonnectra.sh</code> clones the app into <code>local-app/</code> and starts it through the <strong>production</strong> compose file. Idempotent, may be run any number of times.</li><li>Reachable at <code>https://app.qonnectra.localhost</code> (admin: <code>https://admin.qonnectra.localhost/admin</code>, API: <code>https://api.qonnectra.localhost</code>).</li><li>Two accounts, credentials in <code>local-app/deployment/.env</code>, generated randomly on the first run. <strong>Never write them into docs, tests, scripts or commits</strong> – always read them through <code>process.env</code> resp. the <code>.env</code> file. <ul><li><code>APP_USER_USERNAME</code> / <code>APP_USER_PASSWORD</code> – account <strong>without</strong> administration rights, group from <code>APP_USER_GROUP</code> (default <code>Editor</code>: all domain data editable, no access to <code>/admin/*</code>). <strong>The default for all captures</strong> – part A describes the view of ordinary users. The setup creates resp. updates it on every run.</li><li><code>DJANGO_SUPERUSER_USERNAME</code> / <code>DJANGO_SUPERUSER_PASSWORD</code> – Django superuser for administration. Only use it for images of <code>/admin/*</code>: it bypasses every permission check and additionally sees the „Logs“ menu entry.</li></ul></li><li><code>PUBLIC_DOCUMENTATION_URL</code> in <code>.env</code> is the help link the app shows in the header and the navigation bar; the setup sets it to <code>https://qonnectra.de/</code> on every run (overridable via <code>QONNECTRA_DOCUMENTATION_URL</code>). If the variable is empty, the app hides the link and it is missing from the image.</li><li>HTTPS runs through a local dev CA; run <code>scripts/install-local-ca.sh</code> once, or alternatively set <code>ignoreHTTPSErrors: true</code> in Playwright.</li><li>Demo data: project <strong>„Testprojekt“</strong> from <code>scripts/qonnectra-demo-data/testprojekt-export.json</code>, imported automatically during setup. Select it in the top left after logging in.</li><li><code>local-app/</code> is gitignored (foreign checkout) – never commit it and only change it through the setup script.</li></ul><p><strong>Playwright setup in the docs repo</strong></p><p>All runs go exclusively against the local instance. There is no configurable target address and no <code>.env</code> in the repo root any more – <code>GEODOCK_URL</code> is no longer read.</p><ul><li><p><code>playwright/local-app.ts</code> is the single source for address and credentials and reads <code>local-app/deployment/.env</code> (<code>APP_DOMAIN</code>, <code>API_DOMAIN</code>, <code>APP_USER_*</code>, <code>DJANGO_SUPERUSER_*</code>). Only obtain credentials through <code>localApp()</code>, never write them into specs, output or commits.</p></li><li><p>Login uses the account <strong>without</strong> administration rights by default. <code>QONNECTRA_LOGIN=admin pnpm test:e2e</code> switches to the superuser – only for areas that stay hidden from ordinary users. Images from an admin run otherwise show an interface that does not exist for the audience of part A.</p></li><li><p><code>playwright/auth.setup.ts</code> runs as a setup project automatically before every spec: it checks reachability (with a pointer to <code>scripts/setup-local-qonnectra.sh</code> if the stack is down), logs in through <code>POST /api/v1/auth/login/</code> and writes <code>auth-state.json</code>. <code>pnpm test:e2e:setup</code> runs only this step.</p></li><li><p><code>auth-state.json</code> is <strong>not</strong> reusable and is regenerated per run: the access token lives for 15 minutes, and the backend rotates refresh tokens with a blacklist (<code>ROTATE_REFRESH_TOKENS</code> + <code>BLACKLIST_AFTER_ROTATION</code>).</p></li><li><p>The setup pins the state the images depend on: cookie <code>selected-project=2</code> („Testprojekt“; a UI login would write <code>1</code> = „Default“) as well as <code>PARAGLIDE_LOCALE=de</code>, <code>mode=light</code>, <code>basemapTheme</code>, <code>mapCenter</code> and <code>mapZoom</code> in <code>localStorage</code>. The map has no auto-fit – without <code>mapCenter</code>/<code>mapZoom</code> (EPSG:3857) it starts at zoom 2 in the Atlantic.</p></li><li><p>One spec per manual chapter: <code>tests/&lt;NN&gt;-&lt;chapter-slug&gt;.spec.ts</code> with a comment naming the chapter it belongs to (see <code>tests/05-karte.spec.ts</code>). The chapter slug stays German because it mirrors the manual&#39;s file name. Videos of a chapter sit next to it in <code>tests/&lt;NN&gt;-&lt;chapter-slug&gt;-video.spec.ts</code>; a separate file is mandatory, because <code>test.use({ video: … })</code> is only allowed at file level (&quot;forces a new worker&quot; inside a <code>test.describe</code> group).</p></li><li><p>Output goes to <code>tests/screenshots/&lt;chapter-slug&gt;/&lt;name&gt;.png</code> through <code>shotPath()</code> resp. <code>tests/videos/&lt;chapter-slug&gt;/&lt;name&gt;.webm</code> through <code>videoPath()</code>. <code>tests/screenshots/</code>, <code>tests/videos/</code>, <code>test-results/</code>, <code>playwright-report/</code> and <code>auth-state.json</code> are gitignored – those are raw captures, not the files of the manual.</p></li><li><p><code>pnpm screenshots:publish</code> (<code>scripts/publish-screenshots.sh</code>) publishes them to <code>public/images/manual/…</code> resp. <code>public/videos/…</code> and converts images to JPEG in the process (quality 85, lowered until the file is under 1.2 MB); videos are only copied, cropping and encoding are done by the spec. The target comes from the manual itself: the script looks for the reference <code>/images/manual/&lt;part&gt;/&lt;name&gt;.jpg</code> resp. <code>/videos/&lt;name&gt;.webm</code> in <code>manual/</code>. Captures without a reference are skipped, so that nothing ends up in the wrong folder. <code>--dry-run</code> shows beforehand what would be created and what replaced, <code>--videos</code> and <code>--images</code> restrict the run to one kind.</p></li><li><p>Only patterns 1 and 2 go through fully automatically. Images with hand-drawn annotations (pattern 3) are post-processed after publishing – look at <code>--dry-run</code> first, otherwise the run overwrites the handwork with a raw capture. To renew only a video, use <code>--videos</code>.</p></li><li><p>Chapter 3 is the only one that also needs the logged-out state: the images of the login page sit in a <code>test.describe</code> block with <code>test.use({ storageState: { cookies: [], origins: [] } })</code>, the images of the interface next to it in the normal logged-in state. The app redirects logged-in calls of <code>/login</code> to <code>/map</code>.</p></li><li><p><code>workers: 1</code> and <code>fullyParallel: false</code> are deliberate: all specs share one instance including project selection and map position.</p></li><li><p>Determinism helpers in <code>playwright/manual-shots.ts</code>: <code>disableAnimations()</code> (transitions and text caret off), <code>moveCursorAway()</code> (no hover states in the image), <code>spotlight()</code> for pattern 2 and <code>composite2x2()</code> for pattern 4. The grid is assembled in the browser, so the repo needs no image library. Pattern 3 (hand-drawn ellipses/arrows) stays post-processing. <code>spotlight()</code> takes one target or a list of targets and exposes each of them; a target is either a locator or a <code>SpotlightEllipse</code> in CSS pixels of the viewport. The ellipse is meant for everything that has no element: trenches, addresses and nodes are drawn into the canvas by the map. Where it sits is measured, not hard-coded – <code>selectedMapFeature()</code> in <code>tests/05-karte.spec.ts</code> searches the canvas for the selection colour of the app (<code>DEFAULT_SELECTED_COLOR</code> = <code>#fff700</code>) and aligns the ellipse to the main axis of the found points. That is necessary because several trenches converge below the map centre and a different one is hit on each run; the inclination and length of the line change with it. If a canvas is locked for <code>getImageData</code> by foreign raster tiles, it is skipped – the objects live in a different one anyway. <code>spotlight()</code> places an SVG with a cut-out over the page and does not change the target element. The obvious route via <code>box-shadow: 0 0 0 9999px</code> on the element itself fails here twice over: the scrim is clipped at the nearest ancestor with <code>overflow: hidden</code>, and making the ancestors transparent makes the map (OpenLayers) lose its canvas content on reflow.</p></li><li><p>Video helpers in <code>playwright/manual-videos.ts</code>: <code>showCursor()</code> places a replica mouse cursor into the page, <code>pointAt()</code>, <code>click()</code>, <code>drag()</code> and <code>typeText()</code> move it at hand speed, <code>postProcessVideo()</code> cuts off the page load and the frame edges. Pitfalls that are already solved there and reappear immediately when rebuilding this:</p><ul><li>The cursor must <strong>not</strong> get its own compositor layer (so <code>left</code>/<code>top</code> instead of <code>transform</code>, no <code>will-change</code>). Otherwise Chromium keeps painting it up to date while rasterising the remaining content lags behind – while dragging the info box wider, the cursor ran a good 180 px ahead of the edge.</li><li>The cursor has to listen to <code>pointermove</code> <strong>and</strong> <code>mousemove</code>: the handle of the info box calls <code>preventDefault()</code> in its <code>pointerdown</code>, after which Chromium sends no more <code>mouse</code> events for that pointer.</li><li>Leave around 90 ms per step while dragging. The map hangs off the width of the info box, and OpenLayers repaints on every change (measured ~70 ms); denser events visibly pile up.</li><li>Cutting is done with the ffmpeg that <code>playwright install</code> ships anyway (<code>ffmpegPath()</code>) – no extra tool on the machine. <code>-ss</code> has to come <strong>after</strong><code>-i</code>; before it, ffmpeg only seeks to the last keyframe, and those sit far apart in Playwright&#39;s recording.</li><li>If a capture creates data (e.g. an attachment), the spec removes it again through the API – and with <code>superuserCredentials()</code>. The group „Editor“ of the capture account only has level &quot;edit&quot; on all domain models and may not DELETE (<code>RoleBasedPermission</code>); the API answers with 403.</li></ul></li><li><p>Composite grids are assembled and therefore <strong>not</strong> 3584 × 2240, but 2656 px wide at a height that follows from the map extent of the tiles (most recently 2656 × 1854). The aspect ratio of the assembly cannot be brought to both target dimensions at once; in the manual the images are rendered at 512 px anyway.</p></li><li><p>The map tiles are generated once by <code>scripts/setup-local-qonnectra.sh</code> through Planetiler (region <code>schleswig-holstein</code>, where the test project lies) and stored under <code>~/.local/share/qonnectra-local-tiles/</code> – outside <code>local-app/</code>, so that <code>--reset</code> does not throw them away. The <code>tileserver</code> gets them as a hard link at <code>local-app/deployment/tiles/germany.mbtiles</code> (a bind mount for the file alone fails, because Docker cannot create the mount point inside the read-only mounted <code>/data</code>). Map images therefore show the real vector base map in light mode. If the <code>.mbtiles</code> is missing (run with <code>--skip-tiles</code>, no Java), the <code>tileserver</code> runs in a restart loop and the map falls back to OSM raster tiles.</p></li><li><p>If the API answers with <strong>502</strong> although the backend container is running: after a restart of the backend, <code>nginx</code> has cached its old container IP (nginx log: &quot;Host is unreachable&quot;) and does not resolve it again. Fixed by <code>docker restart qonnectra_nginx_prod</code>. The setup project waits a minute on 5xx responses, because a cold-started stack answers with 502 for a while.</p></li><li><p>The number of canvas elements in the map depends on the tileserver: with vector tiles OpenLayers creates two, in the OSM raster fallback one. So use <code>page.locator(&#39;div.map canvas&#39;).first()</code>.</p></li><li><p>Always set the map position through <code>page.addInitScript()</code>, not through &quot;load, set <code>localStorage</code>, reload&quot;. The app writes <code>mapCenter</code> and <code>mapZoom</code> back on every <code>moveend</code>; if that lands between setting and reloading, the seed is gone and the map starts at the overview. Tests that click on a particular spot then hit nothing and the info box does not open (symptom: <code>#drawer-title</code> not found).</p></li><li><p>The base map layer is independent of object selection: <code>getClickedFeatures</code> filters via <code>layerFilter</code> down to trench, address, node and area (<code>MapInteractionManager.svelte.ts</code>). Whether the tileserver runs therefore has no influence on clicks.</p></li><li><p>Selecting a map object by clicking is not reproducible without help: a trench line is only a few pixels wide, and below it lies the project area whose surface covers the entire network. The same spot yields different trenches or the area depending on the run. Clicking repeatedly does not help (it is not a tile race) – for a deterministic hit, hide the layer „Gebiet“ before the click. Switching it back on afterwards is not possible, because the opened info box covers the legend.</p></li><li><p>The window height of 1120 px is measured against the navigation bar: with all groups expanded it needs 1093 px of content (measured); at the earlier 800 px the group „System“ sat below the visible area and had to be scrolled into view first. If the bar grows past 1120 px, raise the viewport in <code>playwright.config.ts</code> and do <strong>not</strong> collapse groups; that would be a state users have to produce themselves first. The scroll container of the bar is its grid, reachable through <code>div[class*=&quot;grid-rows-[auto_1fr_auto]&quot;]</code> (<code>SideBar.svelte</code>).</p></li></ul><p><strong>The app (context for selectors and routes)</strong></p><p>SvelteKit + Skeleton. The navigation bar is sorted into groups; the labels are short and only unambiguous together with their group (group „Rohr“ → „Verwaltung“ = Rohrverwaltung). Routes and labels:</p><table tabindex="0"><thead><tr><th>Group</th><th>Route → Label</th></tr></thead><tbody><tr><td>„Info“</td><td><code>/dashboard</code> „Dashboard“, <code>/map</code> „Karte“</td></tr><tr><td>„Funktionen“</td><td><code>/fault-simulation</code> „Störungsanalyse“, <code>/post-compaction</code> „Nachverdichtung“, <code>/pipeline-records</code> „Leitungsauskunft“, <code>/valuation</code> „Wertermittlung“</td></tr><tr><td>„Rohr“</td><td><code>/conduit</code> „Verwaltung“, <code>/trench</code> „Zuordnung“, <code>/pipe-branch</code> „Verzweigung“, <code>/house-connections</code> „Mikrorohre“</td></tr><tr><td>„Kabel“</td><td><code>/network-schema</code> „Netzschema“, <code>/trace</code> „Faserweg“</td></tr><tr><td>„Gebäude“</td><td><code>/address</code> „Adressen“</td></tr><tr><td>„System“</td><td><code>/admin/logs</code> „Logs“, <code>/settings</code> „Einstellungen“</td></tr></tbody></table><p>Plus <code>/login</code> without a navigation bar. Which entries appear depends on the permissions (<code>canAccessRoute</code>); as superuser all are visible. With the default capture account (group „Editor“) the entry „Logs“ is missing from the group „System“ – the group consists only of „Einstellungen“ there. <code>/admin/*</code> is the only blocked path; everything without its own <code>RoutePermission</code> entry counts as allowed. Navigation definition: <code>local-app/frontend/src/lib/config/navLinks.ts</code>, UI texts: <code>local-app/frontend/messages/de.json</code>.</p><h2 id="subagents" tabindex="-1">Subagents <a class="header-anchor" href="#subagents" aria-label="Permalink to &quot;Subagents&quot;">​</a></h2><ul><li><code>manual-author</code> – new or extended manual chapters in the style above</li><li><code>screenshot-automation</code> – write and run Playwright specs for screenshots/videos</li><li><code>manual-review</code> – style, consistency and spelling check before committing</li></ul>`,50)])])}const u=t(n,[["render",i]]);export{g as __pageData,u as default};
